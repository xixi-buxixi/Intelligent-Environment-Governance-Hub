# 前端重构计划 - Vue3 + Element Plus + Vite

## 一、需求概述

将现有前端项目重构为 Vue3 + Vite 项目，使用 Element Plus 组件库进行组件化开发，保持原有环保绿色主题样式。

## 二、页面架构

```
front-end/
├── index.html              # 入口HTML
├── vite.config.js          # Vite配置
├── package.json            # 依赖配置
├── src/
│   ├── main.js             # 入口文件
│   ├── App.vue             # 根组件
│   ├── assets/             # 静态资源
│   │   ├── images/         # 图片资源
│   │   └── styles/         # 全局样式
│   │       ├── variables.css   # CSS变量（主题色等）
│   │       └── global.css      # 全局样式
│   ├── components/         # 公共组件
│   │   ├── AppHeader.vue   # 头部导航组件
│   │   └── ...
│   ├── views/              # 页面视图
│   │   ├── IndexView.vue   # 登录+主页（状态切换）
│   │   ├── SystemView.vue  # 系统管理模块
│   │   ├── DataFetchView.vue   # 数据获取模块
│   │   └── AssistantView.vue   # 环保小助手模块
│   ├── router/             # 路由配置
│   │   └── index.js
│   ├── store/              # 状态管理（可选）
│   │   └── user.js
│   └── utils/              # 工具函数
│       ├── request.js      # Axios封装
│       └── auth.js         # 认证相关
└── public/                 # 公共资源
```

## 三、核心实现方案

### 3.1 技术栈
- Vue 3.4+
- Vue Router 4
- Element Plus 2.5+
- Axios
- Vite 5

### 3.2 登录+主页状态切换方案

`IndexView.vue` 通过 `isLogin` 状态控制显示登录视图或主页视图：

```vue
<template>
  <!-- 登录视图 -->
  <LoginForm v-if="!isLoggedIn" @login-success="handleLoginSuccess" />

  <!-- 主页视图 -->
  <div v-else>
    <AppHeader title="智能环境治理中枢平台" />
    <!-- 主页内容 -->
  </div>
</template>
```

**优点**：
- 无需页面刷新
- 状态保持更流畅
- 代码分离清晰

### 3.3 路由配置

```javascript
const routes = [
  {
    path: '/',
    name: 'index',
    component: () => import('@/views/IndexView.vue')
  },
  {
    path: '/system',
    name: 'system',
    component: () => import('@/views/SystemView.vue'),
    meta: { requiresAuth: true, requiresAdmin: true }
  },
  {
    path: '/data-fetch',
    name: 'dataFetch',
    component: () => import('@/views/DataFetchView.vue'),
    meta: { requiresAuth: true }
  },
  {
    path: '/assistant',
    name: 'assistant',
    component: () => import('@/views/AssistantView.vue'),
    meta: { requiresAuth: true }
  }
]
```

### 3.4 公共组件拆分

| 组件名 | 说明 | 使用页面 |
|--------|------|----------|
| `AppHeader.vue` | 顶部导航栏 | 所有页面 |
| `LoginForm.vue` | 登录表单 | IndexView |
| `ModuleCard.vue` | 功能模块卡片 | HomeView |
| `UserTable.vue` | 用户表格 | SystemView |
| `UserDialog.vue` | 用户编辑弹窗 | SystemView |

### 3.5 样式处理

**CSS变量定义** (`variables.css`)：
```css
:root {
  --primary-color: #67c23a;
  --primary-dark: #2e7d32;
  --primary-light: #a5d6a7;
  --bg-gradient: linear-gradient(135deg, #2e7d32 0%, #4caf50 100%);
}
```

**保持原有样式**：将现有 CSS 迁移到 Vue 组件的 `<style>` 中，保持环保绿色主题。

### 3.6 Axios封装

```javascript
// utils/request.js
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

const request = axios.create({
  baseURL: '/api',
  timeout: 15000
})

// 请求拦截器 - 添加Token
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器 - 处理错误
request.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      localStorage.removeItem('userInfo')
      ElMessage.error('登录已过期，请重新登录')
      router.push('/')
    }
    return Promise.reject(error)
  }
)

export default request
```

## 四、实施步骤

### 步骤1：创建项目基础结构
- 创建 `vite.config.js`
- 更新 `package.json`
- 创建 `src/main.js` 入口文件
- 创建 `src/App.vue` 根组件

### 步骤2：配置路由和状态管理
- 创建 `src/router/index.js`
- 创建用户状态管理（可选，或直接使用localStorage）

### 步骤3：迁移公共组件
- 创建 `AppHeader.vue` 头部组件
- 提取公共样式到 `global.css`

### 步骤4：实现 IndexView（登录+主页）
- 创建 `IndexView.vue`
- 拆分 `LoginForm` 组件
- 实现登录/主页状态切换

### 步骤5：迁移系统管理模块
- 创建 `SystemView.vue`
- 拆分 `UserTable.vue`、`UserDialog.vue` 组件
- 保持原有功能逻辑

### 步骤6：迁移数据获取模块
- 创建 `DataFetchView.vue`
- 保持数据源展示、获取表单、历史记录功能

### 步骤7：迁移环保小助手模块
- 创建 `AssistantView.vue`
- 保持聊天界面、流式响应功能

### 步骤8：测试验证
- 登录/登出流程
- 路由跳转
- 各模块功能

## 五、关键文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `vite.config.js` | 新建 | Vite配置 |
| `package.json` | 修改 | 添加构建脚本和依赖 |
| `src/main.js` | 新建 | Vue入口文件 |
| `src/App.vue` | 新建 | 根组件 |
| `src/router/index.js` | 新建 | 路由配置 |
| `src/utils/request.js` | 新建 | Axios封装 |
| `src/assets/styles/variables.css` | 新建 | CSS变量 |
| `src/assets/styles/global.css` | 新建 | 全局样式 |
| `src/components/AppHeader.vue` | 新建 | 头部组件 |
| `src/views/IndexView.vue` | 新建 | 登录+主页 |
| `src/views/SystemView.vue` | 新建 | 系统管理 |
| `src/views/DataFetchView.vue` | 新建 | 数据获取 |
| `src/views/AssistantView.vue` | 新建 | 环保小助手 |

## 六、验证方法

1. **启动开发服务器**：`npm run dev`
2. **测试登录流程**：输入用户名、密码、验证码登录
3. **测试主页切换**：登录后自动切换到主页视图
4. **测试模块跳转**：点击卡片跳转到各模块页面
5. **测试退出登录**：退出后返回登录视图
6. **测试权限控制**：非管理员无法访问系统管理

## 七、实现状态

| 功能模块 | 状态 |
|----------|------|
| Vite配置 | ✅ 完成 |
| Vue Router路由守卫 | ✅ 完成 |
| Axios封装（Token/401处理） | ✅ 完成 |
| CSS变量主题系统 | ✅ 完成 |
| 登录/主页状态切换 | ✅ 完成 |
| 系统管理模块 | ✅ 完成 |
| 数据获取模块 | ✅ 完成 |
| 环保小助手模块 | ✅ 完成 |

## 八、运行命令

```bash
cd front-end
npm run dev      # 开发模式 http://localhost:3000
npm run build    # 生产构建
npm run preview  # 预览生产构建
```